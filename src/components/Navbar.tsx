const Navbar = () => (
  <header className="relative bg-header-mobile md:bg-header-desktop h-[120px] bg-cover bg-no-repeat bg-center bg-primary-cyan flex items-center justify-center"></header>
);

export default Navbar;
